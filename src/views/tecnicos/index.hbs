<div class="card">
    <h2>🔧 Gerenciar Técnicos</h2>
    <a href="/tecnicos/new" class="btn btn-primary">+ Novo Técnico</a>

    {{#if tecnicos}}
        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Nome</th>
                    <th>Especialidade</th>
                    <th>Ações</th>
                </tr>
            </thead>
            <tbody>
                {{#each tecnicos}}
                <tr>
                    <td>{{this.id}}</td>
                    <td>{{this.nome}}</td>
                    <td>{{this.especialidade}}</td>
                    <td>
                        <a href="/tecnicos/edit/{{this.id}}" class="btn btn-edit">Editar</a>
                        <form action="/tecnicos/delete/{{this.id}}" method="POST" style="display:inline;" onsubmit="return confirm('Excluir este técnico?');">
                            <button type="submit" class="btn btn-delete">Excluir</button>
                        </form>
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    {{else}}
        <p>Nenhum técnico cadastrado.</p>
    {{/if}}
</div>